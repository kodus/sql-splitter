export enum TokenType {
    UNKNOWN,
    R_SELECT,
    R_FROM,
    R_INSERT,
    R_UPDATE,
    R_LIMIT,
    R_WHERE,
    R_INNER,
    R_JOIN,
    R_INNER_JOIN,
    R_ON,
    R_OPEN_PARENTHESIS,
    R_CLOSE_PARENTHESIS,
    ALIAS,
    COLUMN,
    TABLE,
    LIMIT,
}
